



CSS
- Relationships:
  - Parent-child
    - generally we want the parent element to set the width
    - generally we want to let child elements to dictate the height
- Tricks:
  - centering `div`s:
    ```
    div.centered {
      width: 450px;
      margin-inline: auto;
    }
    ```

